var mongojs = require('./index');
var db = mongojs('test', ['things', 'languages']);

var start = new Date();
db.languages.find().map(function(x) {
	return x.name;
}, function(err, names) {
	var end = new Date();
	console.log(end.getTime() - start.getTime());
	console.log(names[0]);
});
